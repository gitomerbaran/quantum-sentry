[package]
name = "common"
version = "0.1.0"
edition = "2021"
description = "Quantum Sentinel v2 shared contract layer (events + types)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
